--------------------------------------------------------------
-- auto generate code by xtool,don't change it.
--------------------------------------------------------------
local choose_level = class(xlib.ecs.system)

function choose_level:ctor(name,context)
    
end

return choose_level
