-- ==========================================================================
-- auto generate code.(Don't modify it)
-- create by:xtool.
-- ==========================================================================
generate = generate or {};
generate.feature = generate.feature or {}
local feature = class(generate.feature)

function feature:ctor(name)
  
end

return feature
